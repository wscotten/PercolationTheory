import styled from 'styled-components/native';

const StartContainer = styled.TouchableOpacity`
  flex: 1;
  align-items: center;
  justify-content: center;
  background-color: purple;
`;

export default StartContainer;
